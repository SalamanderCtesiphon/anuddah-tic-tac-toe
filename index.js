// store the gameboard as an array inside of an object

function gameBoard() {
  const game_board = []
  return { game_board }
}

// players are stored as objects as well

function createUser(name) {
  return { name }
}

// an object to control the flow of the game

function gameFlow() {
  return {}
}
